# ROADMAP

功能规划与候选清单。已完成的能力(块级增量同步、版本留档、冲突收件箱、中转、双栏对比、
全局设置——限速/同步时段/保留策略等)见 README「特性」,此处只列待做。

优先级参考:近期 = 小改动大感知;中期 = 有辨识度的差异化功能;远期 = 慢功夫的底层增强。

## 近期(小而准)

- [x] **浏览器桌面通知 + favicon 角标**:收到配对/目录邀请、出现冲突、同步出错时发系统通知,
      favicon 叠数字角标。已有标签页标题联动的基础。
- [x] **配对二维码 / 一次性配对码**:设备 ID 做成二维码或 `syncx://` 一次性配对链接,
      手机扫码即配对,替代手输长 ID。
- [x] **冲突自动策略**:按目录配置冲突自动处理(保留双方 / 新者胜 / 本机优先),
      冲突收件箱只兜底人工处理不了的。
- [x] **磁盘空间守卫**:大批量接收前预估体积,空间不足提前警告或暂停,
      而不是写到一半失败。
- [x] **传输优先级**:右键某文件「优先同步」,插队到队列最前。
- [x] **发现新设备提示**:局域网内发现未配对设备时顶栏冒泡「一键添加」。
- [x] **双落地静默分叉修复**:数据面正确性缺陷,2026-09
      混版本实测揪出,与 CDC 无关(bb97651 同样复现)。机理:设备对之间至多两条
      连接(每方向一条,设计内),每条会话各自挂独立 SyncPeer;发送端广播增量对同
      目录全部 transport 无差别扇出(broadcast.ts),同一份 delta 在两条连接各到一
      次,两条接收管线并发比对**同一份旧** localIndex、各自判定需收、各自落地。第二
      次通常覆盖第一次的同内容,平时无感;但若两次落地之间本地恰好有真实编辑,第二
      次 rename 会把编辑打回远端旧内容(被覆盖的编辑由 snapshotVersion 存档进版本
      目录,实测取证可见),且落地后两侧索引一致、扫描器看不出差异 → 静默永久分叉,
      全程无任何报错。已修(两道锁都在,收侧为主、新旧对端可混跑):
      ① 接收侧目录级单飞台账(peer.ts receiveLedger)——规划 receive/conflict 前
      先按「路径+版本指纹」认领,同版本已被另一条管线认领(保鲜期 5min 内)即跳过;
      落地/中止/放弃/会话拆除均交还认领(releaseClaims),死仓不卡活连接;
      ② 发送侧按设备去重(broadcast.ts / relay.ts)——同一设备的两条 transport
      只发一份增量,来源设备的反向连接不再收中转回声。
      回归:peer.test.ts「单飞台账」4 例 + broadcast.test.ts 去重 4 例。
- [x] **fe80 链路本地反向发现 URL 缺陷**(预存,bb97651 亦在):入站会话学习对端
      源 IP 时可能拿到 IPv6 link-local 地址(`fe80::…%18`),写进 config peers 后
      主动拨号报 `Invalid URL`——链路本地地址须带 %zone 才有意义,且跨网段本就不可
      达。已修:`learnPeerUrl` 识别 fe80::/10(含带 zone 与带方括号形态)一律不产出
      反向 URL(不写 config、不外拨);邀请卡的来源 IP **展示**不受影响,只掐 URL
      构造一头(handshake.test.ts 3 例)。

## 中期(特色差异化)

- [x] **端到端加密目录(不可信节点)**:允许挂一个「只存块、看不懂内容」的备份/中转节点,
      密钥由目录口令派生。对标 Syncthing「不受信任设备」。
- [x] **文件时间机器**:在版本留档上加「单文件版本时间轴 + 任意两版内容 diff」,
      以及整目录一键回滚到某时刻(版本 + 墓碑数据都在)。
- [x] **忽略规则可视化编辑器**:`.syncxignore` 的 UI 编辑 + 实时测试器
      (粘路径立即显示命中哪条规则、是硬忽略还是可解除;diff 结果里已有 rule 字段可复用)。
- [x] **浏览器内文件管理器**:浏览共享目录内容、下载单个文件、直接删除,
      Web UI 变成远程文件门户——NAS 场景刚需。
- [x] **浏览器内终端**:独立 `/terminal` 路由页,xterm.js + node-pty 伪控制台的完整终端
      (全屏程序/彩色/选区复制粘贴);node-pty 不可用时(单文件分发)自动降级为逐行 REPL。
- [x] **同步事件 Webhook / 飞书机器人**:同步完成、冲突、错误推送到 webhook,
      补齐「无人值守 NAS」的通知闭环。
- [x] **同步周报**:近 7 天流量、冲突、错误的汇总卡片(历史数据已在库里)。
- [x] **计费网络/电池感知**(Windows 端):检测到计费网络或低电量自动挂起数据面,
      恢复后自动续传。
- [ ] **断点续传(重启存活)**:传输中已收块缓冲只在内存,daemon 重启后大文件从
      头重拉。把已收块持久化到共享目录旁的暂存区,重启后按块哈希逐个自校验续传——
      块哈希自校验天然抗缓存篡改/错块,续传正确性不依赖落盘顺序。
- [ ] **抽样审计(drift 哨兵)**:定期与对端抽样比对若干块哈希,把「双落地覆盖」
      这类静默分叉在成灾前 surfaced。是修复项的天然纵深检查:即使闸门失守,审计也
      能在下一轮报「两侧内容不一致」而非沉默。抽样量可配、闲时执行,以微小流量换
      「内容一致」而非仅「索引一致」的确信。

## 远期(底层增强)

- [ ] **QUIC 传输**:弱网/丢包链路下比 TCP 明显更稳,块传输协议可平移。
      2026-09 调研结论:暂缓。① Node 官方二进制(24 LTS/26 current)均未编译
      `node:quic`(`process.features.quic=false`),内置路线不存在;
      ② js-quic(`@matrixai/quic`)是唯一有 win32-x64 预编译的通用绑定,
      主线停更于 2025-03,且原生 .node 无法进单文件发行物(同 node-pty,
      只在旁挂 node_modules 的开发安装形态可用,发行形态不可测);
      ③ `@chainsafe/libp2p-quic` 活跃且带 win32-msvc,但深绑 libp2p
      (multiaddr/Ed25519/libp2p-TLS),无法独立取用。
      等 Node 内置 QUIC 进入官方发行版后重启此项。
- [ ] **块内二进制 delta**:在 1MB 块之内再做局部差异传输(rolling hash 匹配 + 补丁),
      进一步压掉「巨型文件小改动」时的整块重传。2026-09 调研结论:暂缓。
      ① 现有传输已是块级增量(内容寻址块哈希,改动只波及所在 1MB 块),
      块内 delta 仅惠及「单文件局部小改且该块恰好巨大」的场景,收益边际;
      ② 真 delta 需要接收方先把旧块的弱指纹(rolling checksum)送上行——
      这是数据面上一个全新的双向往返 + 补丁引擎,而 BlockRequest/BlockResponse
      目前是「一问一答一块」的单向语义,改造落在正确性最关键的路径上;
      ③ 握手无能力协商(hello 只带版本号,dev 构建为 'dev' 不可比),
      兼容旧对端只能靠 request-driven 门控(旧对端永不发 delta 请求)兜住,
      可做但每条响应都要背双形态(全块/delta)的回退分支;
      ④ 综合估计 ~500–900 行含测试,风险/收益比不如前列候选。
      2026-09 进展:CDC 内容定义分块已落地(见下条),「改动波及定长 1MB 块」
      的场景已消失,块内 delta 的收益被消掉大半 —— 剩余受益场景收窄为
      「同一变长块(均值约 1MB、上限 4MB)内的多处小散改」。重启条件改为:
      出现实测「大文件高频小改」的带宽痛点反馈,且确认改动跨变长块的次数
      仍让传输量比实际改动大一个数量级。
- [x] **块格式升级:内容定义分块(CDC)**:块从定长 1MB 改为滚动哈希
      (32 位旋转窗 Buzhash)切分的变长内容块(256KB–4MB,均值约 1MB),
      块边界随内容而非偏移 —— 6MB 文件中部插 10KB 只重传含插入点的
      一块(实测 <2MB 过网,定长口径≈整文件)。纯附加兼容:`cdh`/`clens`
      与 `blocks` 并存、握手零协商(旧对端永不宣告 CDC 视图,方案对它就
      不触发);本地无旧版可比对时退回定长全量,与旧行为一致。
      2026-09 混版本实测(v0.3.2 发行物 ↔ dev,双向):旧对端永不宣告 `cdh`,
      新端对它只用定长布局、过网量 ≈ 整文件,内容逐字节收敛正确;旧端读不到未知
      字段也不报错。兼容矩阵成立,但实测同时暴露两个**预存**缺陷(双落地静默分叉、
      fe80 反向发现 URL),已单列近期项。
- [x] **按需同步(稀疏文件)**:先同步索引不落盘,打开文件时再拉块——
      小盘设备(如 Android)的刚需大功能。
- [x] **多实例集中管理**:一个 UI 管理多个 daemon(fleet 视图),
      已有控制面 API + token 的基础。
